# Evita que se abran las vistas directamente por URL (solo deben cargarse vía PHP).
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Deny from all
</IfModule>
